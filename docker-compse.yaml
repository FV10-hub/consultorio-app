version: '3'

services:
  todosDB:
    image: postgres:16
    container_name: consultorio-db
    restart: always
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=manager
    volumes:
      - ./postgres:/var/lib/postgresql/data